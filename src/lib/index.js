const setConsoleMessage = require('./setConsoleMessage');
const setErrorResponse = require('./setErrorResponse');
const schemaTypes = require('./schemaTypes');
const util = require('./util');

module.exports = {
  setConsoleMessage,
  setErrorResponse,
  schemaTypes,
  util,
};
